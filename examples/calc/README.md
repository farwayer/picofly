# calc

The demo from [picofly.dev](https://picofly.dev), as a standalone app. Three
cells read three different things, and each one counts its own renders. Press
`a++` and only the cells that read `a` count up.

```sh
yarn start
```
